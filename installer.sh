# Installer only works on ubuntu ( apt pkg manager).
# But u can install all pkgs yourself:

# Golang
# Makefile
# net tools ( optional )
# nano
# screen
# python

apt upgrade -y
apt install golang-go
apt install net-tools
apt install make
apt install nano
apt install screen
apt intsall python